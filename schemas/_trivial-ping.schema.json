{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://jura.ai/contracts/trivial-ping.schema.json",
  "title": "TrivialPing",
  "description": "A deliberately trivial schema whose only job is proving the codegen pipeline (JSON Schema -> Zod + Pydantic) works end-to-end before the real, harder schemas (good-law-status, citation, authority-identity) are built on top of it. Not consumed by any repo.",
  "type": "object",
  "properties": {
    "message": {
      "type": "string",
      "description": "An arbitrary text payload."
    },
    "count": {
      "type": "integer",
      "description": "An arbitrary integer payload."
    }
  },
  "required": ["message", "count"],
  "additionalProperties": false
}
